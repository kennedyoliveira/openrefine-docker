version: '3.8'

services:
  openrefine:
    image: kennedyoliveira/openrefine:latest
    ports:
      - "3333:3333"
    volumes:
      - ./data:/data
    environment:
      - JAVA_TOOL_OPTIONS=-Xmx2G
    restart: unless-stopped
